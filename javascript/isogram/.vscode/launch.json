var _ = require("lodash")
var Immutable = require("Immutable")

var Isogram = function(word) {
    this.word = word;
}

Isogram.prototype.isIsogram = function() {
    const charMap = Immutable.Seq.of(...this.word).reduce((c, r) => {
        update(c, 0, value => {
            value + 1;
            return r;
        }), Immutable.Map()
    });
    return charMap.every((v, k) => v === 1)
}
module.exports = Isogramattach ",
"name": "Attach to Process",
"port": 5858
}
]
}